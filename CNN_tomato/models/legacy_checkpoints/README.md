# Legacy tomato checkpoints

The historical tomato checkpoint files were not included in the original
repository snapshot. Place the published `.pt` files here to rerun
`scripts/evaluate/evaluate_cnn_ensemble.py`.
